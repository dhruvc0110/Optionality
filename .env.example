# Placeholder for LAYER 4 only. The app is backend-less (D15) — nothing reads this yet.
# When live broker data arrives, these keys go into the SERVERLESS PROXY's environment
# (a single cloud function), NOT the PWA bundle and NOT a .env shipped to the browser.
# You create your own Alpaca paper account + keys — see HANDOFF.md.

# --- Alpaca paper (build/test) ---
ALPACA_API_KEY=
ALPACA_API_SECRET=
ALPACA_BASE_URL=https://paper-api.alpaca.markets
